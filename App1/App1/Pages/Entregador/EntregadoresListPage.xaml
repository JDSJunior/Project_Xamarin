<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App1.Pages.EntregadoresPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="ListViewEntregadores" HasUnevenRows="True" SeparatorVisibility="Default" SeparatorColor="Blue">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="5,0,5,0" Orientation="Vertical">
                                <Label Text="Id:"/>
                                <Label Text= "{Binding Id}" TextColor="Black" FontSize="Medium"/>
                                <Label Text="{Binding Nome}" TextColor="Blue" FontSize="Large"/>
                                <Label Text="{Binding Telefone}" TextColor="Green" FontSize="Small"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>